---
"@primavista/sulu": patch
---

`suluPlugins()` switches alignment on for the `style` attribute of a Sulu text editor config, the key sulu/sulu#9091 uses now. `align` from the earlier draft keeps working. `SULU_DEFAULT_CONFIG` lists `style`.
